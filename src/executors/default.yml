---
description: "Default executor for trailing whitespace checks"

docker:
  - image: <<parameters.image>>

resource_class: <<parameters.resource_class>>

parameters:
  image:
    type: string
    default: "cimg/python:3.14"

  resource_class:
    type: enum
    default: "small"
    enum: ["small", "medium", "medium+", "large"]
